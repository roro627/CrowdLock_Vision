fastapi==0.125.0
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.12.0
opencv-python-headless==4.12.0.88
ultralytics==8.3.240
# Needed for local installs because `torch==...+cpu` wheels are hosted on PyTorch index, not PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
torchvision==0.24.1+cpu
# OpenCV 4.12 pins numpy <2.3.0; keep within that range.
numpy==2.2.6
pyyaml==6.0.3
